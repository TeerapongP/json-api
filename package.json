{
  "name": "json-server",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port 9000",
    "start:en": "json-server --watch db-en.json --port 9000"
  },
  "dependencies": {
    "clone": "^2.1.2",
    "json-server": "^0.16.1"
  }
}
